# Scanlyr API — widen pydantic/fastapi pins so newer interpreters (e.g. Python 3.14)
# can resolve wheels for pydantic-core. Pin exact versions in deployment via pip freeze if needed.

fastapi>=0.115.6
uvicorn[standard]>=0.32.1
python-multipart>=0.0.17

sqlalchemy>=2.0.36,<3
psycopg[binary]>=3.2.13
alembic>=1.14.0

pydantic>=2.11.7
pydantic-settings>=2.7.0

python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
email-validator>=2.0.0

httpx>=0.28.1

pytest>=8.3.0

reportlab>=4.2.0
