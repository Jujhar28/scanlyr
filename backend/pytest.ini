[pytest]
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore:.*HTTP_422_UNPROCESSABLE_ENTITY.*:DeprecationWarning

markers =
    integration: uses PostgreSQL (set DATABASE_URL via .env or TEST_DATABASE_URL before pytest).
