import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI event",
};

export default function DetectionDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
