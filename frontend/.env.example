# Copy to `.env.local` for local development (never commit secrets).

# Browser-visible API base (FastAPI)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Product label in chrome (optional)
NEXT_PUBLIC_APP_NAME=Scanlyr
