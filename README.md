# audit-base

Minimal **base** branch (shares Git history with main) so GitHub can open a PR.

Use PR **base: udit-base**, **compare: udit/full-scan** for a full-tree diff / CodeRabbit pass. Do not merge these into main.
